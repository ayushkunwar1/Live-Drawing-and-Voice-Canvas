{
  "name": "live-drawing-voice-canvas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
  "express": "^5.2.1",
  "ws": "^8.18.3"
}
}
